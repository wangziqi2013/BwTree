language: cpp
compiler:
    - g++

install:
    # get c++11 support
    - export CXX="g++-4.9" CC="gcc-4.9"
addons:
    apt:
        sources:
            - ubuntu-toolchain-r-test
            - boost-latest
        packages:
            - gcc-5
            - g++-5

script:
    - make benchmark-all
